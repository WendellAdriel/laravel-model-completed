version: '3'

services:
  app:
    image: wendelladriel/laravel-model-completed
    container_name: laravel-model-completed
    stdin_open: true
    tty: true
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/usr/src/app
